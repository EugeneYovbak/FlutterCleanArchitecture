name: flutter_clean_architecture
description: Flutter with clean architecture application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^3.1.0+1

  # Functional Programming Thingies
  dartz: ^0.8.8

  # Remote API
  connectivity: ^0.4.5+3
  http: ^0.12.0+2

  # Local Cache
  shared_preferences: ^0.5.4+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:

  uses-material-design: true
